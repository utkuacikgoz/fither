# Briefs

The nine product briefs live here, unchanged, as reference — one file per
brief (`brief-0.md` … `brief-8.md`). They are read-only context: agents
quote them, never edit them.

Not yet added. When adding them:

- Paste Brief 2 verbatim into
  `.claude/skills/fither-domain/references/engine-spec.md` (it overrides
  every `PROPOSED` default there).
- Reconcile Brief 1 with
  `.claude/skills/fither-domain/references/movement-schema.md` and
  `scripts/validate-movements.mjs` (patterns, schema fields).
- Resolve the `DECIDE:` markers in the skills (daily prompt questions,
  pricing, point values) with ADRs.
